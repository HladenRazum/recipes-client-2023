import React from "react";
import LoginForm from "./LoginForm";

type Props = {};

const Login = (props: Props) => {
  return (
    <div>
      <h2>Login page</h2>
      <LoginForm />
    </div>
  );
};

export default Login;
